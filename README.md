# TO DOs
(list old -> new)






* Design users#index view
* Deploy to Heroku - v

# IDEAs

*
*

# TIPs
* Merge feature branch:
git checkout master
git pull origin master
git merge test
git push origin master
